apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-joker.fullname" . }}-test-connection"
  labels:
    {{- include "hello-joker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-joker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
